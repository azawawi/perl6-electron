#!/usr/bin/env perl6

use v6;

use lib 'lib';

use Atom::Electron::BrowserWindow;

my $win = Atom::Electron::BrowserWindow.new;
$win.on('ready', sub {
  "Window open!".say;
});

$win.on('closed', sub {
  "Window closed!".say;
});
